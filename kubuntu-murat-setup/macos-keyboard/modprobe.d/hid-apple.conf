# Match macOS function-key behaviour on Apple HID keyboards:
# F-keys are media/brightness by default; hold Fn for F1–F12.
#
# Do not swap Option/Command — the Magic Keyboard already has
# Command next to the spacebar.
options hid_apple fnmode=1 swap_opt_cmd=0 swap_ctrl_cmd=0
